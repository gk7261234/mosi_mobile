export enum ProcessEnv {
  'all',
  'development',
  'production',
}

export enum MenuMode {
  horizontal = 'horizontal',
  vertical = 'vertical',
}
export enum RouteEnum {
  homepage = '/',
  aboutUs = '/aboutUs',
  animation = '/animation',
  game = '/game',
  news = '/news',
  join = '/join',
  contact = '/contact',
}

export enum CountRoma {
  'I',
  'II',
  'III',
  'IV',
  'V',
  'VI',
}
export enum Edirection {
  left,
  right,
}

export enum EStatus {
  normal,
  active,
  disabled,
}
